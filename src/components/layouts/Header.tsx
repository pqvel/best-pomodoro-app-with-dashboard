import { FC, useState } from "react"

const Header: FC = () => {
  const [count, setCount] = useState<number>(0)

  return (
    <>
      <header>
        
      </header>
    </>
  )
}

export default App